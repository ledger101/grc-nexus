---
phase: 14-advanced-analytics-forecasting-anomaly-detection-and-data-export
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/analytics/forecast.test.ts
  - lib/analytics/anomaly.test.ts
autonomous: true
requirements:
  - BRIDGE-ANA-01
  - BRIDGE-ANA-02

must_haves:
  truths:
    - "Running npm test produces failing red tests for linearRegression and forecastPoints before any implementation exists"
    - "Running npm test produces failing red tests for isAnomaly, mean, and stddev before any implementation exists"
    - "Test files import from @/lib/analytics/forecast and @/lib/analytics/anomaly using the verified Vitest pattern"
  artifacts:
    - path: "lib/analytics/forecast.test.ts"
      provides: "Wave 0 test stubs for BRIDGE-ANA-01 forecast utility"
      exports: ["describe(linearRegression)", "describe(forecastPoints)"]
    - path: "lib/analytics/anomaly.test.ts"
      provides: "Wave 0 test stubs for BRIDGE-ANA-02 anomaly detection"
      exports: ["describe(isAnomaly)", "describe(mean)", "describe(stddev)"]
  key_links:
    - from: "lib/analytics/forecast.test.ts"
      to: "lib/analytics/forecast.ts"
      via: "import from @/lib/analytics/forecast"
      pattern: "from '@/lib/analytics/forecast'"
    - from: "lib/analytics/anomaly.test.ts"
      to: "lib/analytics/anomaly.ts"
      via: "import from @/lib/analytics/anomaly"
      pattern: "from '@/lib/analytics/anomaly'"
---

<objective>
Create Wave 0 failing test stubs for the two pure TypeScript utility modules that Plans 02 and 04 will implement. These tests enforce the RED phase of TDD — they must fail (module-not-found) before implementation and turn green after implementation.

Purpose: Nyquist compliance — every automated verify command in Plans 02 and 04 has a test file to run against. Without these stubs, `npm test -- lib/analytics/forecast` would silently pass with zero tests and give false green signal.

Output: Two test files covering all behaviors required by BRIDGE-ANA-01 and BRIDGE-ANA-02, with specific expected values so Plan 02/04 executors know exactly what to implement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-RESEARCH.md
@.planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-VALIDATION.md

<interfaces>
<!-- Exact test pattern from existing codebase — copy this structure. -->

From tests/files/checksum.test.ts (verified analog):
```typescript
import { describe, it, expect } from 'vitest'
import { computeSHA256, verifyChecksum } from '@/lib/files/checksum'

describe('SHA-256 checksum', () => {
  it('returns a 64-character hex string', () => { ... })
  it('returns the same hash for the same input', () => { ... })
})
```

From vitest.config.ts (verified — @ alias resolves to project root):
- `@/lib/analytics/forecast` resolves to `lib/analytics/forecast.ts`
- No manual setup import needed — `tests/setup.ts` is auto-loaded via vitest.config.ts

Verified function signatures to test against (from RESEARCH.md Pattern 1 and PATTERNS.md):
```typescript
// lib/analytics/forecast.ts (will be created in Plan 02)
export function linearRegression(values: number[]): { slope: number; intercept: number }
export function forecastPoints(readings: number[], horizon: number): { lower: number; upper: number }[]

// lib/analytics/anomaly.ts (will be created in Plan 04)
export function mean(values: number[]): number
export function stddev(values: number[], mu?: number): number
export function isAnomaly(value: number, mean: number, stddev: number, threshold?: number): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create forecast.test.ts — RED phase tests for linearRegression and forecastPoints</name>
  <files>lib/analytics/forecast.test.ts</files>
  <read_first>
    - .planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-PATTERNS.md (section: lib/analytics/forecast.test.ts)
    - .planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-RESEARCH.md (section: Pattern 1 Linear Regression Utility)
    - .planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-VALIDATION.md
  </read_first>
  <behavior>
    - linearRegression([0, 1, 2, 3]) → slope = 1, intercept = 0 (exact values)
    - linearRegression([5, 5, 5, 5]) → slope = 0, intercept = 5 (all-same edge case per RESEARCH.md Pitfall 2 guard)
    - linearRegression([2, 4, 6, 8]) → slope = 2, intercept = 2
    - forecastPoints([2, 4, 6, 8], 2) → array of length 2
    - forecastPoints([2, 4, 6, 8], 2) → each element has lower and upper keys
    - forecastPoints([2, 4, 6, 8], 2) → lower less than or equal to upper on each element
    - forecastPoints([5, 5, 5, 5], 2) → lower equals upper equals 5 (zero residual stddev → band collapses to point)
  </behavior>
  <action>
    Create `lib/analytics/forecast.test.ts` following the exact pattern from `tests/files/checksum.test.ts`:

    1. Header: `import { describe, it, expect } from 'vitest'`
    2. Import: `import { linearRegression, forecastPoints } from '@/lib/analytics/forecast'`
       - NOTE: This file does NOT exist yet — the import will fail at test run time producing module-not-found. This is the correct RED state.
    3. Create `describe('linearRegression', ...)` block with it() tests for:
       - Known slope/intercept: `linearRegression([0, 1, 2, 3])` expect `{ slope: 1, intercept: 0 }` (use `toBeCloseTo` with precision 5 for floating-point safety)
       - All-same values: `linearRegression([5, 5, 5, 5])` expect `slope` toBe(0) and `intercept` toBeCloseTo(5, 5)
       - Positive slope: `linearRegression([2, 4, 6, 8])` expect `slope` toBeCloseTo(2, 5)
    4. Create `describe('forecastPoints', ...)` block with it() tests for:
       - Returns array of length equal to horizon: `forecastPoints([2, 4, 6, 8], 2).length` toBe(2)
       - Each element has lower and upper: check `result[0]` has keys `lower` and `upper`
       - lower <= upper on each: `result[0].lower` toBeLessThanOrEqual(`result[0].upper`)
       - Zero-residual case: `forecastPoints([5, 5, 5, 5], 2)[0].lower` toBeCloseTo(5, 5) and `.upper` toBeCloseTo(5, 5)

    Do NOT add a file comment referencing the implementation — keep the test self-contained.
    Run `npm test -- lib/analytics/forecast` to confirm the test file is picked up but fails with module-not-found or equivalent error (RED state confirmed).
  </action>
  <verify>
    <automated>npm test -- lib/analytics/forecast 2>&1 | head -30</automated>
  </verify>
  <done>
    - `lib/analytics/forecast.test.ts` exists
    - `npm test -- lib/analytics/forecast` runs and fails (module-not-found or import error) — NOT silently passes with 0 tests
    - Test file contains describe blocks for `linearRegression` and `forecastPoints`
    - grep confirms: `grep -n "describe\|it(" lib/analytics/forecast.test.ts | wc -l` returns 9 or more (7 it() + 2 describe)
  </done>
  <acceptance_criteria>
    - File exists at `lib/analytics/forecast.test.ts`
    - `grep "from '@/lib/analytics/forecast'" lib/analytics/forecast.test.ts` returns a match
    - `grep -c "it(" lib/analytics/forecast.test.ts` returns 7 or more
    - `npm test -- lib/analytics/forecast` exits non-zero (RED — implementation missing)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create anomaly.test.ts — RED phase tests for mean, stddev, and isAnomaly</name>
  <files>lib/analytics/anomaly.test.ts</files>
  <read_first>
    - .planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-PATTERNS.md (section: lib/analytics/anomaly.test.ts)
    - .planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-RESEARCH.md (section: Pattern 3 Anomaly Detection Cron Route — stats computation)
    - .planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-VALIDATION.md
  </read_first>
  <behavior>
    - mean([2, 4, 6, 8]) → 5
    - mean([10, 10, 10]) → 10
    - stddev([2, 4, 6, 8]) → 2.236… (population stddev of [2,4,6,8] = sqrt(5))
    - stddev([5, 5, 5, 5]) → 0 (identical values — zero stddev guard)
    - isAnomaly(20, 5, 2, 2) → true (|20-5| = 15 > 2*2 = 4)
    - isAnomaly(6, 5, 2, 2) → false (|6-5| = 1 <= 2*2 = 4)
    - isAnomaly(99, 5, 0, 2) → false (stddev = 0, guard returns false regardless of deviation)
  </behavior>
  <action>
    Create `lib/analytics/anomaly.test.ts` following the exact pattern from `tests/files/checksum.test.ts`:

    1. Header: `import { describe, it, expect } from 'vitest'`
    2. Import: `import { mean, stddev, isAnomaly } from '@/lib/analytics/anomaly'`
       - NOTE: This file does NOT exist yet — import will fail (RED state).
    3. `describe('mean', ...)` block:
       - `mean([2, 4, 6, 8])` toBe(5)
       - `mean([10, 10, 10])` toBe(10)
    4. `describe('stddev', ...)` block:
       - `stddev([5, 5, 5, 5])` toBe(0) — identical values
       - `stddev([2, 4, 6, 8])` toBeCloseTo(Math.sqrt(5), 5) — population stddev
    5. `describe('isAnomaly', ...)` block:
       - Value at >2σ: `isAnomaly(20, 5, 2, 2)` toBe(true)
       - Value within 2σ: `isAnomaly(6, 5, 2, 2)` toBe(false)
       - stddev=0 guard: `isAnomaly(99, 5, 0, 2)` toBe(false) — CRITICAL guard from RESEARCH.md Pitfall 2

    Run `npm test -- lib/analytics/anomaly` to confirm RED state (module-not-found).
  </action>
  <verify>
    <automated>npm test -- lib/analytics/anomaly 2>&1 | head -30</automated>
  </verify>
  <done>
    - `lib/analytics/anomaly.test.ts` exists
    - `npm test -- lib/analytics/anomaly` runs and fails (RED — implementation missing)
    - Test file contains describe blocks for `mean`, `stddev`, and `isAnomaly`
    - grep confirms isAnomaly stddev=0 guard test is present
  </done>
  <acceptance_criteria>
    - File exists at `lib/analytics/anomaly.test.ts`
    - `grep "from '@/lib/analytics/anomaly'" lib/analytics/anomaly.test.ts` returns a match
    - `grep -c "it(" lib/analytics/anomaly.test.ts` returns 7 or more
    - `grep "isAnomaly(99" lib/analytics/anomaly.test.ts` returns a match (stddev=0 guard test present)
    - `npm test -- lib/analytics/anomaly` exits non-zero (RED)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → lib utilities | Test files import not-yet-existing modules; module resolution must fail cleanly without false green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-W0-01 | Tampering | Test stubs | accept | Tests are Wave 0 scaffolding only — no production code; no trust boundary crossed |
| T-14-W0-02 | Spoofing | False green from 0-test runs | mitigate | Verify `npm test -- lib/analytics/forecast` exits non-zero; if it exits 0 with "no tests found", add a `it.todo()` to force failure |
</threat_model>

<verification>
Both test files must produce non-zero exit codes when run with `npm test` (RED state before implementation).

```bash
npm test -- lib/analytics/forecast
npm test -- lib/analytics/anomaly
```

Both commands must fail. If either exits 0 with "no tests found", the Wave 0 guard is broken.
</verification>

<success_criteria>
- `lib/analytics/forecast.test.ts` exists with 7+ it() calls covering linearRegression (slope/intercept/edge-case) and forecastPoints (length/shape/zero-residual)
- `lib/analytics/anomaly.test.ts` exists with 7+ it() calls covering mean, stddev, and isAnomaly including the stddev=0 guard
- Both files import from `@/lib/analytics/forecast` and `@/lib/analytics/anomaly` respectively
- `npm test -- lib/analytics/forecast` exits non-zero (RED)
- `npm test -- lib/analytics/anomaly` exits non-zero (RED)
</success_criteria>

<output>
After completion, create `.planning/phases/14-advanced-analytics-forecasting-anomaly-detection-and-data-export/14-01-SUMMARY.md`
</output>
