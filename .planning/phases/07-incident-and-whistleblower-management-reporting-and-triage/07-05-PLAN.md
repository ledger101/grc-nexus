---
phase: 07-incident-and-whistleblower-management-reporting-and-triage
plan: 05
type: execute
wave: 4
depends_on:
  - 07-03
  - 07-04
files_modified:
  - app/(protected)/layout.tsx
autonomous: false
requirements:
  - INCD-01
  - INCD-02
  - INCD-03
  - INCD-04
  - INCD-05
must_haves:
  truths:
    - "Authorized users can discover the incident module from protected navigation."
    - "Anonymous intake and investigator triage workflows are validated end-to-end before phase closure."
  artifacts:
    - path: "app/(protected)/layout.tsx"
      provides: "Incident navigation entry integrated into protected shell"
      contains: "href='/incidents'"
  key_links:
    - from: "app/(protected)/layout.tsx"
      to: "app/(protected)/incidents/page.tsx"
      via: "Primary nav route entry"
      pattern: "href=\"/incidents\""
---

<objective>
Wire incident module navigation and run phase 7 end-to-end human verification.

Purpose: Ensure incident workflows are discoverable and validated before phase close.
Output: Sidebar nav update plus checkpoint verification outcomes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/(protected)/layout.tsx
@app/(protected)/incidents/page.tsx
@app/(protected)/incidents/report/page.tsx
@app/(protected)/incidents/cases/page.tsx
@app/(protected)/incidents/cases/[id]/page.tsx
@app/api/incidents/intake/route.ts
@app/api/incidents/escalate/route.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add incident nav entry in protected layout</name>
  <files>app/(protected)/layout.tsx</files>
  <action>Add `Incident` nav link to `/incidents`, following existing sidebar styling and role-visibility conventions used for compliance/audit. Keep nav gating as affordance only; route and action authorization remain authoritative.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Incident module is discoverable from protected navigation for intended roles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate phase 7 workflows in browser</name>
  <files>app/(protected)/incidents/report/page.tsx, app/(protected)/incidents/report/IncidentReportForm.tsx, app/(protected)/incidents/page.tsx, app/(protected)/incidents/cases/page.tsx, app/(protected)/incidents/cases/[id]/page.tsx, app/(protected)/incidents/cases/[id]/status/StatusUpdateForm.tsx, app/api/incidents/escalate/route.ts</files>
  <action>Execute the verification script below in a running local environment and confirm all expected outcomes before approving phase closure.</action>
  <what-built>Incident intake API/UI, investigator dashboard/list/detail, triage assignment, status progression, closure summary enforcement, and escalation endpoint were implemented in plans 07-01 through 07-04.</what-built>
  <how-to-verify>
    1. Open `/incidents/report` and submit a named incident with category + description + optional contact; confirm redirect to `/incidents/submitted` with case reference.
    2. Submit an anonymous incident and verify confirmation appears without collecting/displaying reporter identity.
    3. Open `/incidents` and verify dashboard metrics and case preview cards load for authorized roles.
    4. Open `/incidents/cases`, filter by status/category, and open a case detail page.
    5. In triage form, assign/reassign investigator and update severity/category; confirm changes persist.
    6. In status update form, move case through `new -> assigned -> in_investigation -> escalated`; verify invalid transitions are not offered.
    7. Attempt to close without resolution summary; verify validation blocks closure.
    8. Provide resolution summary and close case; verify closed state is saved.
    9. Call `POST /api/incidents/escalate` without `x-cron-secret` and confirm 401; call with valid header and confirm success JSON.
    10. Validate a non-assigned non-admin account cannot view restricted case detail due to RLS/route enforcement.
  </how-to-verify>
  <verify>
    <automated>npm run dev</automated>
  </verify>
  <done>All verification steps pass, including anonymous confidentiality checks and closure-summary enforcement, and the user provides explicit approval.</done>
  <resume-signal>Type "approved" or describe issues to resolve.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar navigation -> incident routes | UI visibility is not equivalent to authorization |
| Human verification -> release decision | Incomplete checks can allow confidentiality regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | E | nav-to-route assumptions | mitigate | Keep server-side route/action authorization independent from nav rendering |
| T-07-16 | R | phase completion confidence | mitigate | Require explicit end-to-end checkpoint covering anonymous and triage flows |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Human checkpoint is explicitly approved.
</verification>

<success_criteria>
- Incident workflows are navigable and verified for INCD-01 through INCD-05.
</success_criteria>

<output>
After completion, create `.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-05-SUMMARY.md`
</output>
