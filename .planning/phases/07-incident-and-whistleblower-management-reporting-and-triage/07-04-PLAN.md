---
phase: 07-incident-and-whistleblower-management-reporting-and-triage
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - app/(protected)/incidents/page.tsx
  - app/(protected)/incidents/cases/page.tsx
  - app/(protected)/incidents/cases/IncidentCasesTable.tsx
  - app/(protected)/incidents/cases/[id]/page.tsx
  - app/(protected)/incidents/cases/[id]/IncidentCaseDetail.tsx
  - app/(protected)/incidents/cases/[id]/triage/TriageForm.tsx
  - app/(protected)/incidents/cases/[id]/status/StatusUpdateForm.tsx
autonomous: true
requirements:
  - INCD-03
  - INCD-04
  - INCD-05
must_haves:
  truths:
    - "Authorized users can view incident dashboard and case list scoped to assigned/allowed visibility."
    - "Investigators and triage roles can assign/reassign cases and progress status through legal transitions."
    - "Case closure UI enforces non-empty resolution summary before status can become closed."
  artifacts:
    - path: "app/(protected)/incidents/page.tsx"
      provides: "Incident dashboard route with status and overdue metrics"
      contains: "force-dynamic"
    - path: "app/(protected)/incidents/cases/IncidentCasesTable.tsx"
      provides: "Filterable incident case table with status/category/assignee controls"
      exports: ["IncidentCasesTable"]
    - path: "app/(protected)/incidents/cases/[id]/IncidentCaseDetail.tsx"
      provides: "Case detail with timeline, triage controls, status updates, and closure summary"
      exports: ["IncidentCaseDetail"]
    - path: "app/(protected)/incidents/cases/[id]/status/StatusUpdateForm.tsx"
      provides: "Status transition form including closure summary requirement"
      exports: ["StatusUpdateForm"]
  key_links:
    - from: "app/(protected)/incidents/page.tsx"
      to: "lib/incidents/queries.ts"
      via: "dashboard and preview datasets"
      pattern: "getIncidentDashboardStats"
    - from: "app/(protected)/incidents/cases/[id]/status/StatusUpdateForm.tsx"
      to: "lib/incidents/actions.ts"
      via: "status and close mutations"
      pattern: "updateIncidentStatus"
    - from: "app/(protected)/incidents/cases/[id]/triage/TriageForm.tsx"
      to: "lib/incidents/actions.ts"
      via: "assignment/reassignment mutation"
      pattern: "assignIncidentInvestigator"
---

<objective>
Deliver investigator and triage UI workflows for incident management.

Purpose: This plan implements INCD-03/04/05 observable behavior in protected routes.
Output: Incident dashboard, case list, case detail, assignment form, and status/closure forms.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-RESEARCH.md
@app/(protected)/audit/page.tsx
@app/(protected)/audit/findings/page.tsx
@app/(protected)/audit/findings/[id]/AuditFindingDetail.tsx
@app/(protected)/audit/findings/[id]/status/StatusUpdateForm.tsx
@lib/incidents/queries.ts
@lib/incidents/actions.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build incident dashboard and cases list with role-aware filtering</name>
  <files>app/(protected)/incidents/page.tsx, app/(protected)/incidents/cases/page.tsx, app/(protected)/incidents/cases/IncidentCasesTable.tsx</files>
  <action>Create `/incidents` dashboard and `/incidents/cases` list using existing protected route patterns. Use query helpers for status metrics, overdue counts, and case previews. Build `IncidentCasesTable` with filters for status, category, severity, and investigator. Keep route guards aligned to incident view roles and rely on RLS for final dataset scoping.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Authorized users can view and filter incident cases and dashboard metrics without page reload.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build case detail route and triage assignment form</name>
  <files>app/(protected)/incidents/cases/[id]/page.tsx, app/(protected)/incidents/cases/[id]/IncidentCaseDetail.tsx, app/(protected)/incidents/cases/[id]/triage/TriageForm.tsx</files>
  <action>Create case detail page with immutable case metadata, confidentiality flags, event timeline, and assignee context. Add triage form for assignment/reassignment and severity/category refinement for triage roles. Keep investigator-only and triage-role-only controls visibly separated in UI copy and permissions.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Case detail and triage operations are available to authorized users with clear role boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement status transition and closure form with required summary</name>
  <files>app/(protected)/incidents/cases/[id]/status/StatusUpdateForm.tsx</files>
  <behavior>
    - Test 1: legal transitions are selectable from current status only.
    - Test 2: selecting closed requires entering resolution summary before submit.
    - Test 3: successful updates refresh list/detail/dashboard views.
  </behavior>
  <action>Build a status update form mirroring existing audit transition UX, but with incident status graph and closure narrative enforcement. Require `resolution_summary` for close operations and show validation error when missing. Trigger route revalidation and success feedback on update.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>INCD-04/05 workflow is complete: cases move through lifecycle and cannot close without narrative resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protected UI -> mutation actions | Authenticated users may attempt transitions/assignments beyond their authority |
| Case detail rendering -> confidential fields | UI can accidentally expose sensitive fields to non-authorized roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | E | triage and status forms | mitigate | Role checks in actions and limited control rendering in UI per active role |
| T-07-13 | I | case detail projections | mitigate | Only request/render fields required for role workflows; rely on RLS filtered rows |
| T-07-14 | T | close-case workflow | mitigate | Require non-empty `resolution_summary` and enforce in shared schema/action validation |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `/incidents`, `/incidents/cases`, and case detail routes load for authorized roles.
- Close transition blocks submission without resolution summary.
</verification>

<success_criteria>
- INCD-03, INCD-04, and INCD-05 are operational through protected dashboard/list/detail workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-04-SUMMARY.md`
</output>
