---
phase: 07-incident-and-whistleblower-management-reporting-and-triage
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/incidents/actions.ts
  - lib/incidents/queries.ts
  - lib/incidents/escalation.ts
  - app/api/incidents/escalate/route.ts
  - app/api/incidents/intake/route.ts
autonomous: true
requirements:
  - INCD-01
  - INCD-02
  - INCD-03
  - INCD-04
  - INCD-05
must_haves:
  truths:
    - "Named and anonymous intake submissions are accepted through validated server paths."
    - "Investigators and triage roles can assign/reassign and progress incident status with transition enforcement."
    - "Case closure is rejected unless a non-empty resolution summary is provided."
  artifacts:
    - path: "lib/incidents/actions.ts"
      provides: "Server actions for create, triage assignment, status updates, and closure with resolution summary"
      exports: ["createIncidentCase", "assignIncidentInvestigator", "updateIncidentStatus", "closeIncidentCase"]
    - path: "lib/incidents/queries.ts"
      provides: "Dashboard/list/detail/escalation selectors for incident workflows"
      exports: ["listIncidentCases", "getIncidentCaseById", "getIncidentDashboardStats", "getIncidentCasesForEscalation"]
    - path: "app/api/incidents/intake/route.ts"
      provides: "Public/anonymous-safe intake endpoint that strips identity for anonymous mode"
      exports: ["POST"]
    - path: "app/api/incidents/escalate/route.ts"
      provides: "CRON-protected escalation endpoint"
      exports: ["POST"]
  key_links:
    - from: "app/api/incidents/intake/route.ts"
      to: "lib/incidents/actions.ts"
      via: "route delegates validated intake creation"
      pattern: "createIncidentCase"
    - from: "lib/incidents/actions.ts"
      to: "lib/schemas/incidents.ts"
      via: "safeParse guards intake/triage/closure payloads"
      pattern: "safeParse"
    - from: "app/api/incidents/escalate/route.ts"
      to: "lib/incidents/escalation.ts"
      via: "cron route dispatches escalation emails"
      pattern: "sendIncidentEscalationEmails"
---

<objective>
Implement phase 7 service layer: intake API, mutation actions, query helpers, and escalation endpoint.

Purpose: Provide the secure backend workflows used by intake and investigator UIs.
Output: Incident services in `lib/incidents/*` plus two API routes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-RESEARCH.md
@lib/compliance/actions.ts
@lib/compliance/queries.ts
@lib/compliance/escalation.ts
@app/api/compliance/escalate/route.ts
@types/auth.ts

<interfaces>
From `types/auth.ts`:
```typescript
export type AppRole =
  | 'admin'
  | 'board-member'
  | 'ceo'
  | 'risk-officer'
  | 'audit-officer'
  | 'compliance-officer'
  | 'dept-head'
```

From `lib/compliance/actions.ts` (pattern to mirror):
```typescript
async function getWriteContext(allowedRoles: AppRole[])
export async function createObligation(values): Promise<ActionResult>
```

From `app/api/compliance/escalate/route.ts` (security gate):
```typescript
const secret = request.headers.get('x-cron-secret')
if (!secret || secret !== process.env.CRON_SECRET) {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement incident mutation actions with anonymity and closure guards</name>
  <files>lib/incidents/actions.ts</files>
  <behavior>
    - Test 1: anonymous intake stores no reporter identity columns and succeeds with required intake data.
    - Test 2: unauthorized users cannot triage or reassign incidents.
    - Test 3: close action fails when resolution summary is empty and passes when provided.
  </behavior>
  <action>Create `lib/incidents/actions.ts` mirroring compliance/audit action style. Implement intake creation for named and anonymous reports, triage assignment/reassignment, status transitions, and closure operation requiring `resolution_summary`. Add role and institution checks, schema validation, and `revalidatePath` fanout for `/incidents` routes.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All mutation entry points exist with schema validation, transition guards, and confidentiality-safe intake handling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement incident queries and escalation dispatcher</name>
  <files>lib/incidents/queries.ts, lib/incidents/escalation.ts</files>
  <action>Build list/detail/dashboard/escalation selectors in `lib/incidents/queries.ts` with investigator and profile joins. Build `lib/incidents/escalation.ts` using Resend + `escapeHtml`, threshold helper, and recipient resolution for assigned investigator plus oversight roles. Keep output payloads typed and ready for table/detail UIs.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Incident query layer and escalation service support dashboard/list/detail rendering and SLA reminders.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement intake and escalation API routes with strict guards</name>
  <files>app/api/incidents/intake/route.ts, app/api/incidents/escalate/route.ts</files>
  <action>Create `POST /api/incidents/intake` for intake submissions, supporting anonymous mode by stripping identity inputs when `is_anonymous=true`. Create `POST /api/incidents/escalate` with `x-cron-secret` guard before any DB work, then query escalation candidates and dispatch emails. Return structured counts for processed/sent/skipped cases.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Routes are callable by UI/scheduler and reject unauthorized usage paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public intake caller -> API route | Untrusted anonymous input can attempt abuse or identity injection |
| Scheduler/HTTP caller -> escalation route | External callers can attempt unauthorized trigger execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | S | `POST /api/incidents/escalate` | mitigate | Require strict `x-cron-secret` validation before any DB/email side effects |
| T-07-06 | T | intake payload | mitigate | Zod validation + explicit identity stripping in anonymous mode |
| T-07-07 | E | incident triage updates | mitigate | Role allowlists and institution checks in action context helper |
| T-07-08 | I | incident detail query outputs | mitigate | Return role-appropriate projections and rely on RLS-scoped clients |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `POST /api/incidents/escalate` without secret returns 401.
- Intake route accepts valid payloads and rejects malformed payloads.
</verification>

<success_criteria>
- Backend capability for INCD-01 through INCD-05 is available to UI workflows with confidentiality-safe enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-02-SUMMARY.md`
</output>
