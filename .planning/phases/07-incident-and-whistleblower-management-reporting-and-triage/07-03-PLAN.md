---
phase: 07-incident-and-whistleblower-management-reporting-and-triage
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - app/(protected)/incidents/report/page.tsx
  - app/(protected)/incidents/report/IncidentReportForm.tsx
  - app/(protected)/incidents/submitted/page.tsx
  - components/incidents/IncidentCategoryBadge.tsx
autonomous: true
requirements:
  - INCD-01
  - INCD-02
must_haves:
  truths:
    - "Users can submit incident reports with category, description, and optional contact information."
    - "Submitters can explicitly choose anonymous mode and identity fields are suppressed in submission payload."
    - "Successful submission yields a case reference confirmation view."
  artifacts:
    - path: "app/(protected)/incidents/report/IncidentReportForm.tsx"
      provides: "Intake form supporting named/anonymous mode and validation"
      exports: ["IncidentReportForm"]
    - path: "app/(protected)/incidents/report/page.tsx"
      provides: "Protected report-intake route shell"
      contains: "force-dynamic"
    - path: "app/(protected)/incidents/submitted/page.tsx"
      provides: "Post-submit confirmation page with case reference"
      contains: "case reference"
  key_links:
    - from: "app/(protected)/incidents/report/IncidentReportForm.tsx"
      to: "app/api/incidents/intake/route.ts"
      via: "form submission to intake endpoint"
      pattern: "/api/incidents/intake"
---

<objective>
Deliver the user-facing incident intake workflow for named and anonymous submissions.

Purpose: This is the primary INCD-01/02 entrypoint for phase 7.
Output: Intake route, form, and submission confirmation page.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-RESEARCH.md
@app/(protected)/compliance/obligations/new/ObligationForm.tsx
@app/(protected)/audit/findings/new/AuditFindingForm.tsx
@lib/schemas/incidents.ts
@app/api/incidents/intake/route.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build report intake page shell and category badge component</name>
  <files>app/(protected)/incidents/report/page.tsx, components/incidents/IncidentCategoryBadge.tsx</files>
  <action>Create a protected route at `/incidents/report` using existing auth guard conventions and `dynamic = 'force-dynamic'`. Add `IncidentCategoryBadge` for consistent category rendering in confirmation and later list/detail views using existing badge token styles.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Report route shell and reusable category badge component compile and are ready for intake form integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement IncidentReportForm with anonymous-mode payload behavior</name>
  <files>app/(protected)/incidents/report/IncidentReportForm.tsx</files>
  <behavior>
    - Test 1: named mode submits reporter/contact fields when provided.
    - Test 2: anonymous mode hides/clears identity fields before submission.
    - Test 3: invalid category/empty description is blocked by client validation.
  </behavior>
  <action>Build RHF + Zod form with fields for title, category, description, optional contact, and an anonymity toggle. When anonymous is enabled, clear reporter identity fields and submit payload with `is_anonymous=true`. Submit to `/api/incidents/intake`, handle success/error toasts, and redirect to confirmation page with returned case reference.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Users can submit named or anonymous incident reports through a validated UI workflow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add post-submit confirmation route with case reference handoff</name>
  <files>app/(protected)/incidents/submitted/page.tsx</files>
  <action>Create `/incidents/submitted` confirmation page that reads case reference from search params/state, displays confidentiality notice, and links users back to dashboard/report page. Keep copy explicit that anonymous submissions do not expose reporter identity to investigators.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Submission flow ends with clear confirmation and case reference visibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form input -> intake endpoint | Untrusted text and optional identity fields enter backend intake path |
| Client anonymity toggle -> payload shaping | UI mistakes could leak identity fields in anonymous submissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | I | anonymous intake payload | mitigate | Explicitly clear identity fields client-side and enforce stripping server-side |
| T-07-10 | T | intake form validation | mitigate | Use shared Zod schema-aligned constraints before submit |
| T-07-11 | R | submission acknowledgement | mitigate | Return and show case reference for user traceability without revealing internal data |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `/incidents/report` loads and submits both named and anonymous samples.
- `/incidents/submitted` renders case reference confirmation.
</verification>

<success_criteria>
- INCD-01 and INCD-02 intake UX is operational and aligned to confidentiality rules.
</success_criteria>

<output>
After completion, create `.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-03-SUMMARY.md`
</output>
