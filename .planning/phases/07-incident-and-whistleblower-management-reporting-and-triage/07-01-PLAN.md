---
phase: 07-incident-and-whistleblower-management-reporting-and-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260523000026_incident_schema.sql
  - supabase/migrations/20260523000027_incident_rls.sql
  - supabase/migrations/20260523000028_incident_triggers.sql
  - types/incidents.ts
  - lib/schemas/incidents.ts
  - lib/incidents/incident-utils.ts
autonomous: true
requirements:
  - INCD-01
  - INCD-02
  - INCD-03
must_haves:
  truths:
    - "Incident and whistleblower data structures exist with explicit anonymous-mode support."
    - "Role-segregated visibility is enforced at the RLS layer for incident case access."
    - "Incident tables are attached to immutable audit triggers without recording submitter identity for anonymous intake."
  artifacts:
    - path: "supabase/migrations/20260523000026_incident_schema.sql"
      provides: "Incident enums, case tables, event/evidence tables, indexes, and storage bucket"
      contains: "create table public.incident_cases"
    - path: "supabase/migrations/20260523000027_incident_rls.sql"
      provides: "RLS policies for case visibility, intake permissions, and investigator scoping"
      contains: "enable row level security"
    - path: "supabase/migrations/20260523000028_incident_triggers.sql"
      provides: "Audit trigger attachment for incident tables"
      contains: "audit.attach_audit_trigger"
    - path: "types/incidents.ts"
      provides: "Incident module types, status/category labels, and role-facing row interfaces"
      exports: ["IncidentCategory", "IncidentStatus", "IncidentCaseRow"]
    - path: "lib/schemas/incidents.ts"
      provides: "Zod contracts for intake, triage, assignment, status updates, and closure"
      exports: ["incidentIntakeSchema", "incidentTriageSchema", "incidentCloseSchema"]
  key_links:
    - from: "lib/schemas/incidents.ts"
      to: "types/incidents.ts"
      via: "schema infer types align with exported domain unions"
      pattern: "z.infer"
    - from: "supabase/migrations/20260523000027_incident_rls.sql"
      to: "supabase/migrations/20260523000026_incident_schema.sql"
      via: "policy definitions target incident tables introduced in schema migration"
      pattern: "incident_cases"
---

<objective>
Create phase 7 database and contract foundations: incident schema, RLS, triggers, shared types, and validation schemas.

Purpose: Every service/UI workflow in this phase depends on these security and data contracts.
Output: Three SQL migrations plus incident domain TypeScript contracts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-RESEARCH.md
@supabase/migrations/20260522000023_audit_schema.sql
@supabase/migrations/20260522000024_audit_rls.sql
@supabase/migrations/20260522000025_audit_triggers.sql
@types/auth.ts
@lib/schemas/audit-findings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define incident contracts first (types, schemas, utility guards)</name>
  <files>types/incidents.ts, lib/schemas/incidents.ts, lib/incidents/incident-utils.ts</files>
  <behavior>
    - Test 1: incident category/status enums validate legal values and reject unknown values.
    - Test 2: close-case schema rejects empty resolution summaries.
    - Test 3: status transition guard enforces new -> assigned -> in_investigation -> escalated -> closed.
  </behavior>
  <action>Create strict incident unions, labels, and row interfaces in `types/incidents.ts`. Add Zod schemas in `lib/schemas/incidents.ts` for intake, triage assignment, investigation update, and closure. Add pure utility guards in `lib/incidents/incident-utils.ts` for status transitions, overdue/escalation thresholds, and deterministic evidence storage paths.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Incident contracts compile and can be reused directly by service and UI plans.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add incident schema migration with anonymous-report-safe fields</name>
  <files>supabase/migrations/20260523000026_incident_schema.sql</files>
  <action>Create incident enums and tables: `incident_cases`, `incident_case_events`, and `incident_case_evidence`. Include explicit anonymity fields (`is_anonymous`, nullable reporter identity columns), investigator assignment fields, SLA due date, and required `resolution_summary` column for closure validation in service layer. Add indexes for institution/status/category/investigator/sla_due_date and create private `incident-evidence` storage bucket.</action>
  <verify>
    <automated>npx supabase migration list</automated>
  </verify>
  <done>Database model supports named and anonymous intake, triage lifecycle, and confidential case operations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add RLS and trigger migrations for confidential visibility and immutable logging</name>
  <files>supabase/migrations/20260523000027_incident_rls.sql, supabase/migrations/20260523000028_incident_triggers.sql</files>
  <action>Enable and force RLS on all incident tables. Implement SELECT policies allowing access only to admin/ceo/compliance-officer and assigned investigator. Restrict writes to intake + triage roles with investigator-specific update paths. Ensure anonymous intake policy does not require authenticated reporter identity and does not enforce identity-capturing fields. Attach `audit.attach_audit_trigger()` to incident tables for immutable event capture.</action>
  <verify>
    <automated>npx supabase db push</automated>
  </verify>
  <done>Incident tables enforce confidentiality by default and are wired to immutable audit trigger coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intake caller -> incident insert path | Untrusted input can attempt identity spoofing or malformed confidential reports |
| Authenticated user -> case queries | Unauthorized roles may attempt to read sensitive cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I | `incident_cases` SELECT | mitigate | Enforce institution + role/assignment predicates in RLS using `public.institution_id()` and investigator checks |
| T-07-02 | E | incident triage updates | mitigate | Restrict update policies to admin/compliance-officer and assigned investigator for allowed transitions only |
| T-07-03 | R | anonymous intake actor trace | mitigate | Anonymous path stores null identity columns and avoids authenticated actor context for anonymous submissions |
| T-07-04 | T | incident event/evidence mutation | mitigate | Attach audit triggers and append-only patterns for event/evidence history |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Supabase migration list includes three new phase 7 migration files.
- `npx supabase db push` applies incident schema and policies.
</verification>

<success_criteria>
- Foundation contracts for INCD-01/02/03 are complete and enforce confidentiality constraints at DB layer.
</success_criteria>

<output>
After completion, create `.planning/phases/07-incident-and-whistleblower-management-reporting-and-triage/07-01-SUMMARY.md`
</output>
