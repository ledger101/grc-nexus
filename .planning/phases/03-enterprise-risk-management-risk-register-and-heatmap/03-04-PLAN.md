---
phase: 03-enterprise-risk-management-risk-register-and-heatmap
plan: 04
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
files_modified:
  - app/(protected)/risk/page.tsx
  - app/(protected)/risk/register/page.tsx
  - app/(protected)/risk/register/RiskRegisterTable.tsx
  - app/(protected)/risk/register/RiskFilterBar.tsx
  - app/(protected)/risk/heatmap/page.tsx
  - app/(protected)/risk/RiskHeatmap.tsx
  - components/ui/tooltip.tsx
  - app/(protected)/layout.tsx
autonomous: true
requirements:
  - RISK-04
  - RISK-06
  - RISK-08
must_haves:
  truths:
    - "Risk register filters category, severity, owner, and status without full-page reload."
    - "Heatmap renders as a live CSS Grid 5x5 matrix with fixed severity zones, not Nivo."
    - "Risk overview and heatmap pages are dynamic protected routes."
    - "Users can navigate to Risk module from protected layout."
  artifacts:
    - path: "app/(protected)/risk/register/RiskRegisterTable.tsx"
      provides: "TanStack v8 register table with client filtering"
    - path: "app/(protected)/risk/RiskHeatmap.tsx"
      provides: "5x5 CSS Grid heatmap component with tooltip risk dots"
    - path: "app/(protected)/risk/heatmap/page.tsx"
      provides: "heatmap screen with legend and data wiring"
  key_links:
    - from: "app/(protected)/risk/register/RiskFilterBar.tsx"
      to: "app/(protected)/risk/register/RiskRegisterTable.tsx"
      via: "TanStack column filter state"
      pattern: "setColumnFilters|getFilteredRowModel"
    - from: "app/(protected)/risk/RiskHeatmap.tsx"
      to: "lib/risk/risk-utils.ts"
      via: "cell severity and scoring"
      pattern: "calculateRiskScore|getRiskSeverity"
---

<objective>
Ship the institutional risk register and live heatmap visualization surfaces so users can monitor risk posture through responsive filters and a standards-compliant 5x5 matrix.

Purpose: Complete the Phase 03 read/monitoring surfaces required by roadmap success criteria.
Output: Overview route, register route, heatmap route, shared UI components, and protected navigation entry.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-RESEARCH.md
@app/(protected)/admin/audit-log/AuditLogTable.tsx
@app/(protected)/strategic/KpiGrid.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build risk register page with TanStack v8 no-reload filters</name>
  <read_first>
    - app/(protected)/admin/audit-log/AuditLogTable.tsx
    - app/(protected)/strategic/KpiGrid.tsx
    - lib/risk/queries.ts
  </read_first>
  <files>app/(protected)/risk/register/page.tsx, app/(protected)/risk/register/RiskRegisterTable.tsx, app/(protected)/risk/register/RiskFilterBar.tsx</files>
  <behavior>
    - Filters apply instantly without full page reload.
    - Filter dimensions are category, severity, owner, status.
    - Table rows show computed score and severity labels.
  </behavior>
  <action>
    Implement server page `app/(protected)/risk/register/page.tsx` with `export const dynamic = 'force-dynamic'`, role gate for `admin|ceo|risk-officer|audit-officer|board-member|dept-head`, and data load through `listRisks`.

    Implement `RiskRegisterTable.tsx` as client component using `@tanstack/react-table` v8 with `getCoreRowModel`, `getSortedRowModel`, and `getFilteredRowModel`. Include columns: title, category, inherent score (LxI), severity badge, residual score, owner, status, treatments count.

    Implement `RiskFilterBar.tsx` as client controls bound to TanStack column filters for `category`, `severity`, `owner`, and `status`; updates must use client state/router params only and never trigger `window.location.reload`.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- app/(protected)/risk/register -t "filter|table|no reload"</automated>
  </verify>
  <acceptance_criteria>
    - Register filtering works for all four dimensions without full reload.
    - Table values show computed severity and score from utility functions.
    - Register page includes force-dynamic export.
  </acceptance_criteria>
  <done>RISK-08 is satisfied by a live filterable register experience.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CSS Grid 5x5 heatmap page and component (no Nivo)</name>
  <read_first>
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-RESEARCH.md
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
    - lib/risk/risk-utils.ts
  </read_first>
  <files>components/ui/tooltip.tsx, app/(protected)/risk/heatmap/page.tsx, app/(protected)/risk/RiskHeatmap.tsx</files>
  <behavior>
    - Heatmap is a 5x5 CSS Grid with fixed severity-zone backgrounds.
    - Orientation is likelihood 5 at top to likelihood 1 at bottom, impact 1..5 left-to-right.
    - Risk dots show tooltips and navigate to detail pages.
  </behavior>
  <action>
    Add `components/ui/tooltip.tsx` (shadcn tooltip primitive) if missing. Implement `app/(protected)/risk/RiskHeatmap.tsx` as client component using CSS Grid (`repeat(5, 64px)` rows/columns, 2px gaps) and severity-zone classes from `risk-utils`; do not add or import Nivo.

    Implement `app/(protected)/risk/heatmap/page.tsx` with `export const dynamic = 'force-dynamic'`, role gate, data fetch from `listRiskHeatmapPoints`, legend display for Low/Medium/High/Critical, and clickable risk dot tooltips containing title + score + severity.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- app/(protected)/risk/heatmap -t "css grid|orientation|tooltip"</automated>
  </verify>
  <acceptance_criteria>
    - Heatmap uses CSS Grid implementation only.
    - Severity zoning and matrix orientation match locked design decisions.
    - Heatmap page is dynamic and protected.
  </acceptance_criteria>
  <done>RISK-06 is satisfied with a live 5x5 matrix view aligned to Phase 03 UI contract.</done>
</task>

<task type="auto">
  <name>Task 3: Wire risk overview route and protected navigation entry</name>
  <read_first>
    - app/(protected)/layout.tsx
    - app/(protected)/strategic/page.tsx
    - app/(protected)/risk/register/page.tsx
  </read_first>
  <files>app/(protected)/risk/page.tsx, app/(protected)/layout.tsx</files>
  <action>
    Create `app/(protected)/risk/page.tsx` with `export const dynamic = 'force-dynamic'`, summary cards (total risks, open risks, overdue treatments, critical risks), mini heatmap preview, and quick links to `/risk/register`, `/risk/heatmap`, and `/risk/new`.

    Update `app/(protected)/layout.tsx` navigation to include a "Risk" entry visible to all authenticated roles that can view risk pages. Keep existing layout patterns and active route styling conventions.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- app/(protected)/risk/page.tsx -t "summary cards|navigation"</automated>
  </verify>
  <acceptance_criteria>
    - Risk overview route renders with live counts and links.
    - Protected sidebar includes Risk module navigation.
    - Overview page is dynamic and role-safe.
  </acceptance_criteria>
  <done>Phase 03 read surfaces are discoverable and accessible through protected navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server data->client filtering | Client-side filter logic must not become security boundary |
| rendered heatmap->navigation | User interactions must route only to authorized records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | I | `app/(protected)/risk/register/page.tsx` | mitigate | Fetch through RLS-protected queries and treat client filters as UX-only, never authorization. |
| T-03-04-02 | S | `app/(protected)/risk/heatmap/page.tsx` | mitigate | Apply server-side role gate and rely on institution-scoped query helpers for every heatmap data fetch. |
| T-03-04-03 | T | `app/(protected)/risk/RiskHeatmap.tsx` | mitigate | Derive cell severity from utility functions to avoid ad-hoc severity drift across views. |
| T-03-04-04 | R | `app/(protected)/layout.tsx` | accept | Navigation visibility by role is UI-level only; DB RLS remains enforcement boundary. |
</threat_model>

<verification>
- Register and heatmap pages pass typecheck and targeted tests.
- Manual route smoke: `/risk`, `/risk/register`, `/risk/heatmap`.
- No Nivo dependencies added and no Nivo imports in risk heatmap files.
</verification>

<success_criteria>
- RISK-06 and RISK-08 are delivered with the locked technical approach.
- Risk module navigation and dashboard surface are complete.
- Heatmap rendering and filtering are responsive without page reload dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-04-SUMMARY.md`.
</output>
