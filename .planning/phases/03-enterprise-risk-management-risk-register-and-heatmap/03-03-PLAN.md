---
phase: 03-enterprise-risk-management-risk-register-and-heatmap
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - app/(protected)/risk/new/page.tsx
  - app/(protected)/risk/new/RiskForm.tsx
  - app/(protected)/risk/[id]/page.tsx
  - app/(protected)/risk/[id]/edit/page.tsx
  - app/(protected)/risk/[id]/edit/RiskEditForm.tsx
  - app/(protected)/risk/[id]/treatments/new/page.tsx
  - app/(protected)/risk/[id]/treatments/new/TreatmentForm.tsx
  - components/risk/MatrixSelector.tsx
  - components/risk/TreatmentStatusSelect.tsx
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04
  - RISK-05
  - RISK-07
must_haves:
  truths:
    - "Authorized users can create and edit risk entries with inherent/residual matrix scoring."
    - "Risk detail shows side-by-side inherent/residual score panels with severity labels."
    - "Treatment rows support inline status updates via Component 28 while overdue rows remain read-only."
    - "All protected risk pages are dynamic and role-gated."
  artifacts:
    - path: "components/risk/MatrixSelector.tsx"
      provides: "5x5 interactive selector with top-row likelihood=5 orientation"
    - path: "components/risk/TreatmentStatusSelect.tsx"
      provides: "inline status update control for treatment rows"
    - path: "app/(protected)/risk/[id]/page.tsx"
      provides: "risk detail UI with treatment list and inline status interactions"
  key_links:
    - from: "app/(protected)/risk/new/RiskForm.tsx"
      to: "lib/risk/actions.ts"
      via: "createRisk submit"
      pattern: "createRisk"
    - from: "components/risk/TreatmentStatusSelect.tsx"
      to: "lib/risk/actions.ts"
      via: "updateRiskTreatmentStatus"
      pattern: "updateRiskTreatmentStatus"
---

<objective>
Deliver the end-to-end risk authoring and detail workflow, including matrix scoring and inline treatment management, so risk officers can execute RISK-01 through RISK-05 and RISK-07 from protected pages.

Purpose: Provide the core operational ERM workflows before register/heatmap aggregation views.
Output: Risk create/edit/detail/treatment pages and reusable risk interaction components.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-VERIFICATION.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-CONTEXT.md
@app/(protected)/strategic/objectives/new/ObjectiveForm.tsx
@app/(protected)/strategic/kpis/new/KpiForm.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build matrix scoring component and risk create/edit forms</name>
  <read_first>
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
    - lib/schemas/risk.ts
    - lib/risk/actions.ts
  </read_first>
  <files>components/risk/MatrixSelector.tsx, app/(protected)/risk/new/page.tsx, app/(protected)/risk/new/RiskForm.tsx, app/(protected)/risk/[id]/edit/page.tsx, app/(protected)/risk/[id]/edit/RiskEditForm.tsx</files>
  <behavior>
    - Matrix uses 5x5 grid with top row likelihood 5 and left-to-right impact 1..5.
    - Create form requires title/category/owner/objective and inherent score pair.
    - Residual score fields are optional but editable on edit form.
  </behavior>
  <action>
    Implement `components/risk/MatrixSelector.tsx` as the shared 5x5 control from UI-SPEC Component 23 with keyboard navigation and `aria-pressed` cell state.

    Build `app/(protected)/risk/new/page.tsx` and `RiskForm.tsx` using `react-hook-form + zodResolver(riskSchema)` with fields: objective, title, description, category, owner, inherent matrix, mitigating controls, optional residual matrix. Submit calls `createRisk` and redirects to `/risk/register`.

    Build edit route `app/(protected)/risk/[id]/edit/page.tsx` + `RiskEditForm.tsx` loading current values and calling `updateRisk`. Add `export const dynamic = 'force-dynamic'` to both protected page files and enforce role allowlist `admin|ceo|risk-officer`.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- app/(protected)/risk -t "matrix|risk form|edit risk"</automated>
  </verify>
  <acceptance_criteria>
    - Matrix selector is reusable and drives both inherent and residual form values.
    - Both create/edit pages are dynamic and role-gated.
    - Form submissions invoke server actions and surface validation errors inline.
  </acceptance_criteria>
  <done>Risk creation and editing workflows are fully functional with enforced matrix scoring conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build risk detail and treatment creation workflows</name>
  <read_first>
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
    - lib/risk/queries.ts
    - lib/risk/actions.ts
  </read_first>
  <files>app/(protected)/risk/[id]/page.tsx, app/(protected)/risk/[id]/treatments/new/page.tsx, app/(protected)/risk/[id]/treatments/new/TreatmentForm.tsx</files>
  <behavior>
    - Detail page shows inherent/residual score cards with computed severity.
    - Treatment creation captures owner, due date, and status.
    - Overdue treatments are visually escalated.
  </behavior>
  <action>
    Implement risk detail page at `app/(protected)/risk/[id]/page.tsx` with score panel (inherent and residual), treatment table section, and links to edit risk/add treatment. Render severity badges from `risk-utils` helpers.

    Implement treatment create flow at `/risk/[id]/treatments/new` using `TreatmentForm.tsx` + `riskTreatmentSchema` and `createRiskTreatment` action.

    Ensure both pages include `export const dynamic = 'force-dynamic'` and redirect unauthorized users to `/risk/register`.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- app/(protected)/risk/[id] -t "score panel|treatment create|overdue styling"</automated>
  </verify>
  <acceptance_criteria>
    - Risk detail presents computed score and severity for inherent/residual domains.
    - Treatment form persists owner/due date/status and returns to detail page.
    - Overdue treatments are clearly marked in detail view.
  </acceptance_criteria>
  <done>Risk-specific treatment lifecycle entry points are available and tied to risk detail context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement inline treatment status select (UI-SPEC Component 28)</name>
  <read_first>
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-VERIFICATION.md
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
    - app/(protected)/admin/users/UserManagementTable.tsx
  </read_first>
  <files>components/risk/TreatmentStatusSelect.tsx, app/(protected)/risk/[id]/page.tsx</files>
  <behavior>
    - Non-overdue rows render editable inline select.
    - Overdue rows render read-only badge (no select).
    - Status update is in-place with no page reload.
  </behavior>
  <action>
    Create `components/risk/TreatmentStatusSelect.tsx` as inline shadcn `Select` control with options `planned`, `in_progress`, `completed`, `cancelled` (exclude `overdue`). Wire `onValueChange` to `updateRiskTreatmentStatus` server action and optimistic row state update.

    Integrate component into risk detail treatment rows in `app/(protected)/risk/[id]/page.tsx`. For rows whose computed/current status is `overdue`, render read-only overdue badge per D-21. Show success/error toasts through Sonner and keep updates on current page state without hard navigation.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- components/risk/TreatmentStatusSelect.test.ts -t "inline update|overdue read-only"</automated>
  </verify>
  <acceptance_criteria>
    - Component 28 exists and is used in detail treatment rows.
    - Inline update succeeds without full page reload.
    - Overdue rows are non-editable and visually distinguished.
  </acceptance_criteria>
  <done>The Phase 03 inline treatment status gap is closed and executable behavior matches UI-SPEC contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form->server actions | Form fields and matrix values are untrusted input |
| browser inline status control->server | High-frequency status updates can be abused for unauthorized transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | `components/risk/MatrixSelector.tsx` + forms | mitigate | Validate all matrix outputs with Zod range checks (1..5) before any server write. |
| T-03-03-02 | E | `app/(protected)/risk/**/page.tsx` | mitigate | Enforce role gate in server pages and server actions; include force-dynamic to avoid stale cached role state. |
| T-03-03-03 | T | `components/risk/TreatmentStatusSelect.tsx` | mitigate | Exclude overdue from editable options and re-check transition server-side in `updateRiskTreatmentStatus`. |
| T-03-03-04 | R | `app/(protected)/risk/[id]/page.tsx` | mitigate | Keep writes routed through audited actions so status and treatment changes remain attributable. |
</threat_model>

<verification>
- Risk create/edit/detail/treatment routes render and submit successfully.
- Inline treatment status updates perform in place and show toasts.
- Typecheck and targeted tests pass.
</verification>

<success_criteria>
- RISK-01/02/03/04/05/07 flows are operable through protected UI pages.
- Inline treatment status control exists and resolves the prior verification gap.
- Overdue treatment read-only rule is enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-03-SUMMARY.md`.
</output>
