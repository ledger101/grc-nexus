---
phase: 03-enterprise-risk-management-risk-register-and-heatmap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260522000011_risk_schema.sql
  - supabase/migrations/20260522000012_risk_rls.sql
  - supabase/migrations/20260522000013_risk_triggers.sql
  - types/supabase.ts
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04
  - RISK-05
  - RISK-07
must_haves:
  truths:
    - "Risk and treatment records exist as institution-scoped tables protected by RLS from first migration."
    - "Risk score storage uses raw dimensions only; computed score is never persisted."
    - "Risk and treatment writes are captured by immutable audit triggers."
    - "Phase execution halts if risk migrations are not applied to Supabase."
  artifacts:
    - path: "supabase/migrations/20260522000011_risk_schema.sql"
      provides: "risk enums, risks table, risk_treatments table, indexes"
    - path: "supabase/migrations/20260522000012_risk_rls.sql"
      provides: "RLS policies using public.institution_id() and public.active_role()"
    - path: "supabase/migrations/20260522000013_risk_triggers.sql"
      provides: "audit trigger attachment for risk tables"
    - path: "types/supabase.ts"
      provides: "typed Supabase row definitions for risks and treatments"
  key_links:
    - from: "supabase/migrations/20260522000012_risk_rls.sql"
      to: "supabase/migrations/20260522000011_risk_schema.sql"
      via: "policies target tables created in schema migration"
      pattern: "alter table public.risks|alter table public.risk_treatments"
    - from: "supabase/migrations/20260522000013_risk_triggers.sql"
      to: "audit.attach_audit_trigger"
      via: "trigger attachment"
      pattern: "select audit.attach_audit_trigger"
---

<objective>
Create the risk data foundation with migration-safe sequencing and hard DB-layer security so all later Phase 03 work runs against real schema, real RLS, and real audit trails.

Purpose: Establish non-negotiable persistence and access-control contracts before any risk UI or actions are built.
Output: Three new migrations (00011/00012/00013), refreshed Supabase types, and a blocking schema-apply gate.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-CONTEXT.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-RESEARCH.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-PATTERNS.md
@.planning/phases/01-foundation-authentication-rls-and-audit-trail/01-SUMMARY.md
@.planning/phases/02-strategic-planning-objectives-and-kpis/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create risk schema migration set 00011/00012/00013 with locked conventions</name>
  <read_first>
    - supabase/migrations/20260522000007_strategic_schema.sql
    - supabase/migrations/20260522000008_strategic_rls.sql
    - supabase/migrations/20260522000009_strategic_triggers.sql
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-CONTEXT.md
  </read_first>
  <files>supabase/migrations/20260522000011_risk_schema.sql, supabase/migrations/20260522000012_risk_rls.sql, supabase/migrations/20260522000013_risk_triggers.sql</files>
  <behavior>
    - Migration numbers are exactly 00011, 00012, and 00013 (never 00010).
    - `public.risks` stores inherent/residual likelihood and impact only; no computed score column.
    - RLS policies use `(select public.institution_id())` and `(select public.active_role())` helpers.
    - Audit trigger attachment exists for both `public.risks` and `public.risk_treatments`.
  </behavior>
  <action>
    Create `20260522000011_risk_schema.sql` with enums `public.risk_category`, `public.risk_status`, `public.treatment_status`; create tables `public.risks` and `public.risk_treatments`; include columns and constraints from D-01..D-08 including nullable residual fields and status defaults. Add indexes: `idx_risks_institution_id`, `idx_risks_objective_id`, `idx_risks_owner_id`, `idx_risks_status`, `idx_risks_category`, `idx_risk_treatments_institution_id`, `idx_risk_treatments_risk_id`, `idx_risk_treatments_owner_id`, `idx_risk_treatments_due_date`, `idx_risk_treatments_status`.

    Create `20260522000012_risk_rls.sql`: enable + force RLS on both tables; add select/insert/update policies with explicit role allowlists using `(select public.active_role())` for `admin`, `ceo`, and `risk-officer` writes; all institution checks must use `(select public.institution_id())` wrappers.

    Create `20260522000013_risk_triggers.sql`: attach audit triggers with `select audit.attach_audit_trigger('risks');` and `select audit.attach_audit_trigger('risk_treatments');` only.
  </action>
  <verify>
    <automated>rg "20260522000011_risk_schema|20260522000012_risk_rls|20260522000013_risk_triggers|public.institution_id\(\)|public.active_role\(\)|attach_audit_trigger\('risks'\)|attach_audit_trigger\('risk_treatments'\)" supabase/migrations</automated>
  </verify>
  <acceptance_criteria>
    - All three migration files exist with exact numbering sequence 00011/00012/00013.
    - `public.risks` has inherent/residual dimensions and no stored computed score column.
    - RLS file contains helper-wrapper predicates for both institution and role checks.
    - Trigger file has exactly two attach statements and no redefinition of audit functions.
  </acceptance_criteria>
  <done>Risk schema, RLS, and trigger migrations are complete and conform to locked Phase 03 decisions D-01..D-13 and D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Supabase-generated types for new risk tables and enums</name>
  <read_first>
    - types/supabase.ts
    - package.json
    - supabase/migrations/20260522000011_risk_schema.sql
  </read_first>
  <files>types/supabase.ts</files>
  <action>
    Regenerate or update `types/supabase.ts` so `Database['public']['Tables']` includes `risks` and `risk_treatments`, and `Database['public']['Enums']` includes `risk_category`, `risk_status`, and `treatment_status`. Keep style and ordering consistent with existing generated sections in the repository.
  </action>
  <verify>
    <automated>rg "risks:|risk_treatments:|risk_category:|risk_status:|treatment_status:" types/supabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - `types/supabase.ts` exposes typed Rows/Inserts/Updates for both new tables.
    - Enum unions match migration values exactly.
    - No unrelated type sections are removed.
  </acceptance_criteria>
  <done>Risk domain types are available for compile-time safety in later plans.</done>
</task>

<task type="auto">
  <name>Task 3: Run blocking schema apply gate before any Phase 03 app code</name>
  <read_first>
    - supabase/migrations/20260522000011_risk_schema.sql
    - supabase/migrations/20260522000012_risk_rls.sql
    - supabase/migrations/20260522000013_risk_triggers.sql
  </read_first>
  <files>supabase/migrations/20260522000011_risk_schema.sql, supabase/migrations/20260522000012_risk_rls.sql, supabase/migrations/20260522000013_risk_triggers.sql</files>
  <action>
    Execute `npx supabase db push` in repo root as a hard gate. If command exits non-zero, stop Phase 03 execution and resolve link/auth/migration errors before continuing. After success, run `npx supabase migration list` to confirm 00011/00012/00013 are applied in order.
  </action>
  <verify>
    <automated>npx supabase db push ; npx supabase migration list</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes with exit code 0.
    - Migration list shows risk migrations as applied after 00010.
    - No later plan starts if this task fails.
  </acceptance_criteria>
  <done>Database state is synchronized and execution is safely unblocked for domain/application work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->server action | Untrusted form payload crosses into privileged write path |
| server->postgres | Role and institution enforcement must remain at DB policy layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | E | `supabase/migrations/20260522000012_risk_rls.sql` | mitigate | Enforce role-gated writes with `(select public.active_role()) in ('admin','ceo','risk-officer')` and institution predicate wrappers. |
| T-03-01-02 | I | `supabase/migrations/20260522000011_risk_schema.sql` | mitigate | Prohibit stored computed score column; compute score only in application logic to avoid stale integrity drift. |
| T-03-01-03 | T | `supabase/migrations/20260522000013_risk_triggers.sql` | mitigate | Attach immutable audit triggers on risk tables so all changes are captured regardless of app bugs. |
</threat_model>

<verification>
- `npx supabase db push` succeeds.
- `npx supabase migration list` shows 00011/00012/00013 applied.
- `npm run lint` and `npm run typecheck` pass after type refresh.
</verification>

<success_criteria>
- Risk schema is present and queryable under RLS.
- Risk writes are auditable via trigger attachment.
- Execution remains blocked until schema application succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-01-SUMMARY.md`.
</output>
