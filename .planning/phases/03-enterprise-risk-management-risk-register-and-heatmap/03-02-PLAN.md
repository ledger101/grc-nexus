---
phase: 03-enterprise-risk-management-risk-register-and-heatmap
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - types/risk.ts
  - lib/risk/risk-utils.ts
  - lib/schemas/risk.ts
  - lib/risk/queries.ts
  - lib/risk/actions.ts
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04
  - RISK-05
  - RISK-07
user_setup:
  - service: resend
    why: "Overdue treatment escalation email delivery"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend dashboard -> API Keys"
      - name: RESEND_FROM_EMAIL
        source: "Verified sender/domain in Resend"
must_haves:
  truths:
    - "Risk score and severity are deterministic pure functions with tested 1-25 boundaries."
    - "Risk and treatment server actions enforce role and institution constraints before writes."
    - "Overdue treatment escalation sends to owner and manager-level recipients without blocking core DB writes."
  artifacts:
    - path: "lib/risk/risk-utils.ts"
      provides: "calculateRiskScore, getRiskSeverity, isTreatmentOverdue"
    - path: "lib/schemas/risk.ts"
      provides: "Zod v3 validation for risk and treatment forms"
    - path: "lib/risk/actions.ts"
      provides: "create/update risk and treatment actions with escalation"
    - path: "lib/risk/queries.ts"
      provides: "institution-safe fetch helpers for risk pages"
  key_links:
    - from: "lib/risk/actions.ts"
      to: "lib/risk/risk-utils.ts"
      via: "overdue detection and severity calculation"
      pattern: "isTreatmentOverdue|getRiskSeverity|calculateRiskScore"
    - from: "lib/risk/actions.ts"
      to: "resend"
      via: "overdue escalation email dispatch"
      pattern: "new Resend|send"
---

<objective>
Implement domain contracts and server-side logic for risk scoring, treatment lifecycle, and escalation so UI plans can bind to stable APIs without reinterpretation.

Purpose: Lock down business behavior and write-path security before frontend assembly.
Output: Risk types, utility module, Zod schemas, query helpers, and server actions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-CONTEXT.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-RESEARCH.md
@.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-UI-SPEC.md
@.planning/phases/02-strategic-planning-objectives-and-kpis/02-06-SUMMARY.md
@types/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define risk domain contracts and pure scoring utilities</name>
  <read_first>
    - types/strategic.ts
    - lib/strategic/kpi-utils.ts
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-CONTEXT.md
  </read_first>
  <files>types/risk.ts, lib/risk/risk-utils.ts</files>
  <behavior>
    - `calculateRiskScore(1,1)` returns 1 and `calculateRiskScore(5,5)` returns 25.
    - Severity map is exact: Low 1-4, Medium 5-9, High 10-15, Critical 16-25.
    - Overdue returns true only when status is not completed/cancelled and due date is in the past.
  </behavior>
  <action>
    Create `types/risk.ts` with exported unions and row-facing DTOs for risk category/status/treatment status and risk register row payloads. Implement `lib/risk/risk-utils.ts` pure functions:
    1) `calculateRiskScore(likelihood: number, impact: number): number`
    2) `getRiskSeverity(score: number): 'low' | 'medium' | 'high' | 'critical'`
    3) `isTreatmentOverdue(status: 'planned'|'in_progress'|'completed'|'overdue'|'cancelled', dueDate: string | Date): boolean` using `date-fns/isPast`.

    Add exported style maps for severity/status badges consumed by register/detail/heatmap components.
  </action>
  <verify>
    <automated>npm test -- lib/risk/risk-utils.test.ts -t "score|severity|overdue"</automated>
  </verify>
  <acceptance_criteria>
    - Utility module has no Next.js/Supabase imports.
    - Boundary cases 4/5/9/10/15/16 map to correct severity.
    - Overdue helper excludes completed/cancelled statuses.
  </acceptance_criteria>
  <done>Core risk logic is deterministic, testable, and reusable across server and client components.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Zod v3 schemas and typed query helpers</name>
  <read_first>
    - lib/schemas/strategic.ts
    - lib/strategic/queries.ts
    - types/supabase.ts
  </read_first>
  <files>lib/schemas/risk.ts, lib/risk/queries.ts</files>
  <behavior>
    - Risk schemas reject likelihood/impact values outside 1..5.
    - Residual fields remain optional/nullable until assessment is provided.
    - Query helpers return only institution-visible rows under RLS.
  </behavior>
  <action>
    Build `lib/schemas/risk.ts` with Zod v3-only APIs for `riskSchema`, `riskUpdateSchema`, `riskTreatmentSchema`, and `inlineTreatmentStatusSchema`. Use preprocess/coerce patterns from Phase 2; enforce required fields for RISK-01 and treatment fields for RISK-05.

    Build `lib/risk/queries.ts` with request-scoped functions that accept `SupabaseClient` and provide: `listRisks`, `getRiskById`, `listRiskTreatments`, `listRiskHeatmapPoints`, `listRiskOwners`, and `listObjectiveOptions`. Include embedded joins to objective title and owner profile name.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- lib/schemas/risk.test.ts -t "1..5|nullable residual|treatment status"</automated>
  </verify>
  <acceptance_criteria>
    - Schemas compile on Zod v3 without v4-only API usage.
    - Query helpers are typed against `types/supabase.ts`.
    - Register, detail, and heatmap data shapes are all available via query helper functions.
  </acceptance_criteria>
  <done>Validation and data-read contracts are complete for all Phase 03 UI routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement risk and treatment Server Actions with escalation dispatch</name>
  <read_first>
    - lib/strategic/actions.ts
    - lib/auth/admin-actions.ts
    - lib/email/send-role-notification.ts
    - .planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-RESEARCH.md
  </read_first>
  <files>lib/risk/actions.ts</files>
  <behavior>
    - Unauthorized roles cannot create or update risks/treatments.
    - Score remains computed in code, not persisted as a dedicated DB column.
    - When a treatment transitions to overdue, escalation email sends to treatment owner and institution managers.
  </behavior>
  <action>
    Create `lib/risk/actions.ts` with `'use server'` and actions: `createRisk`, `updateRisk`, `createRiskTreatment`, `updateRiskTreatmentStatus`, `updateResidualRisk`, `bulkMarkOverdueTreatments` (optional helper for page load sync). Enforce role gate using `user.app_metadata.active_role` in `['admin','ceo','risk-officer']` for writes.

    In `updateRiskTreatmentStatus`, compute overdue via `isTreatmentOverdue`. If overdue transition occurs, dispatch Resend email to:
    - owner email from `auth.users` + `user_profiles`
    - manager recipients as all active `ceo` role users in same institution (concrete manager proxy until dedicated manager_id exists)

    Keep email failure non-fatal: log and continue returning successful DB state. Revalidate `/risk`, `/risk/register`, `/risk/heatmap`, and `/risk/[id]` paths after writes.
  </action>
  <verify>
    <automated>npm run typecheck ; npm test -- lib/risk/actions.test.ts -t "role gate|overdue escalation|revalidate"</automated>
  </verify>
  <acceptance_criteria>
    - All write actions parse with Zod before DB calls.
    - Role gates and institution scoping are enforced in server action code.
    - Overdue escalation attempts both owner and manager-recipient delivery via Resend.
    - Revalidate paths include register and heatmap routes.
  </acceptance_criteria>
  <done>Risk write-path behavior and escalation logic are complete and ready for UI integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->server action | Untrusted input can attempt privilege escalation or malformed score data |
| server action->email provider | External send failures must not corrupt governance records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | E | `lib/risk/actions.ts` | mitigate | Enforce explicit role allowlist from `user.app_metadata.active_role` before any write action. |
| T-03-02-02 | T | `lib/schemas/risk.ts` | mitigate | Validate 1..5 bounds and enum values with Zod v3 schemas before persistence. |
| T-03-02-03 | D | `lib/risk/actions.ts` + Resend | mitigate | Make escalation dispatch non-blocking to preserve DB transaction success on provider outages. |
| T-03-02-04 | I | `lib/risk/risk-utils.ts` | mitigate | Compute score in code from dimensions only; avoid stale or tampered persisted computed values. |
</threat_model>

<verification>
- `npm run typecheck` passes.
- Focused unit tests for `risk-utils`, `riskSchema`, and `risk actions` pass.
- Manual action smoke via node script confirms overdue escalation branch executes.
</verification>

<success_criteria>
- Domain contracts cover create/update/score/treatment/escalation use cases.
- Action APIs are stable for form and table components.
- RISK-01/02/03/04/05/07 logic is implemented at business-layer fidelity.
</success_criteria>

<output>
After completion, create `.planning/phases/03-enterprise-risk-management-risk-register-and-heatmap/03-02-SUMMARY.md`.
</output>
