---
phase: 05-board-management-meetings-and-resolutions
plan: 06
type: execute
wave: 4
depends_on: ["04", "05"]
files_modified:
  - app/(protected)/layout.tsx
  - app/(protected)/dashboard/page.tsx
  - app/(protected)/role-select/RoleSelectForm.tsx
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - BOARD-01
  - BOARD-02
  - BOARD-03
  - BOARD-04
  - BOARD-05
  - BOARD-06
must_haves:
  truths:
    - "Board nav item (Gavel icon) appears in sidebar for all VIEW_ROLES including board-secretary, hidden from dept-head"
    - "board-secretary appears in ROLE_LABELS and ROLE_BADGE_COLORS in dashboard and role-select (with consistent color)"
    - "Full end-to-end human verification of meeting lifecycle, resolution recording, action tracking, escalation simulation, and immutability passes"
    - "ROADMAP.md updated with 6 plans for Phase 5 and all [x] checked"
  artifacts:
    - path: "app/(protected)/layout.tsx"
      provides: "Board nav link with Gavel, conditional render for VIEW_ROLES"
    - path: "app/(protected)/dashboard/page.tsx + RoleSelectForm.tsx"
      provides: "board-secretary added to role maps"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 plans listed and marked complete"
  key_links:
    - from: "layout.tsx"
      to: "05-UI-SPEC.md"
      via: "Gavel icon + visibility rules from D-41"
---
<object ive>
Integrate the Board module into global navigation and role displays, then perform the final human verification checkpoint for the entire Phase 5 capability.

Purpose: Last plan. Makes Board discoverable and consistent with existing modules. The checkpoint gate ensures the complete user journey (create → pack → resolution → action → close → immutable) works before marking phase complete.

Output: Updated layout + role files, ROADMAP, and verified Phase 5.
</objective>

<execution_context>
@C:\Users\Kuziwa\.config\kilo\get-shit-done\workflows\execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-board-management-meetings-and-resolutions/05-CONTEXT.md
@.planning/phases/05-board-management-meetings-and-resolutions/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Board nav item to protected layout + update role labels (D-41, D-03, D-04)</name>
  <files>
app/(protected)/layout.tsx,
app/(protected)/dashboard/page.tsx,
app/(protected)/role-select/RoleSelectForm.tsx
  </files>
  <action>
Edit layout.tsx:
- Import { Gavel } from 'lucide-react'
- Add after Compliance or Risk:
  {['admin','ceo','board-member','board-secretary','audit-officer','risk-officer'].includes(activeRole) && (
    <Link href="/board" ...>
      <Gavel className="mr-2 inline h-4 w-4" />
      Board
    </Link>
  )}

Edit the two role files:
- Add 'board-secretary': 'Board Secretary' to ROLE_LABELS
- Add 'board-secretary': 'bg-indigo-700 text-white border-indigo-700' (or matching gold/navy theme from UI-SPEC) to ROLE_BADGE_COLORS
- Keep exhaustive checks happy (TypeScript will complain if missed)

  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "nav + roles updated"</automated>
  </verify>
  <done>Board nav visible exactly to VIEW_ROLES, hidden from dept-head. board-secretary appears in all role badges/labels with distinct color. tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All of Phase 5: DB schema + RLS + triggers + types + schemas + utils + actions + queries + escalation + document route + dashboard + full meeting lifecycle UI + nav integration</what-built>
  <how-to-verify>
1. Log in as board-secretary (or admin)
2. Click new "Board" nav item (Gavel)
3. Dashboard shows stats (0s ok for first run)
4. Click "Schedule Meeting" → create with 3 agenda items + 2 attendees
5. Upload a PDF board pack (client SHA computed, server stores + verifies)
6. Change status to In Progress
7. Record a resolution (choose proposer/seconder from attendees, vote Passed) — see RES-001
8. Create an action item from the resolution with tomorrow due date
9. Close the meeting (confirm dialog appears, irreversible warning)
10. Verify: edit/resolution/upload buttons gone or disabled, "CLOSED" red banner visible
11. Attempt to edit closed meeting via direct action — receives "Meeting is closed" error
12. View /board/actions — action item listed with owner/due
13. (Simulate) run the escalate endpoint with CRON_SECRET header — verify email would be sent (or check logs)
14. Log in as risk-officer — can see meetings but cannot see/download board packs (403 or hidden)
15. Log in as dept-head — Board nav not visible
16. All pages use correct fonts, spacing, colors, 44px targets per 05-UI-SPEC
  </how-to-verify>
  <resume-signal>Type "approved" (or describe any issues found) to continue to final ROADMAP update</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md with Phase 5 plans (post-verification)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read the Phase 5 section, replace the placeholder with:

**Plans:** 6 plans

Plans:
- [x] 05-01-PLAN.md — Database migrations (role extension + schema + RLS + triggers) + types + Zod schemas + board-utils + supabase db push
- [x] 05-02-PLAN.md — Server Actions + queries + escalation service + /api/board/escalate route
- [x] 05-03-PLAN.md — Board pack document download Route Handler with SHA-256 verification
- [x] 05-04-PLAN.md — Board dashboard (stats + upcoming meetings + overdue actions) reusing ComplianceStatCard
- [x] 05-05-PLAN.md — Meetings list + create/edit + detail tabs (Agenda/Documents/Resolutions/Actions) + all forms + upload + resolution + actions
- [x] 05-06-PLAN.md — Board nav integration (Gavel) + role badge updates + full human verification checkpoint

Mark Phase 5 status as complete with today's date.

  </action>
  <verify>
    <automated>grep -A 20 "Phase 5: Board Management" .planning/ROADMAP.md | grep -E "05-0[1-6]|6 plans"</automated>
  </verify>
  <done>ROADMAP.md accurately lists all 6 Phase 5 plans as complete with correct descriptions.</done>
</task>

</tasks>

<threat_model>
... (nav/role display only — low risk)
</threat_model>

<verification>
Full human sign-off + ROADMAP updated + git status shows all new PLAN.md + ROADMAP change.
</verification>

<success_criteria>
- Board nav present and correctly gated
- board-secretary role visible everywhere
- Human verification passes all 16 steps (or documented deviations)
- ROADMAP updated and committed
- Phase 5 marked complete
</success_criteria>

<output>
After final task, create 05-06-SUMMARY.md with verification notes, any issues found/fixed, and confirmation that all BOARD-01 through BOARD-06 success criteria are observable.
</output>
