---
phase: 05-board-management-meetings-and-resolutions
plan: 05
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - app/(protected)/board/meetings/page.tsx
  - app/(protected)/board/meetings/MeetingsTable.tsx
  - app/(protected)/board/meetings/new/page.tsx
  - app/(protected)/board/meetings/new/MeetingForm.tsx
  - app/(protected)/board/meetings/[id]/page.tsx
  - app/(protected)/board/meetings/[id]/MeetingDetailTabs.tsx (or inline tabs)
  - app/(protected)/board/meetings/[id]/edit/page.tsx
  - app/(protected)/board/meetings/[id]/documents/upload/page.tsx
  - app/(protected)/board/meetings/[id]/documents/upload/DocumentUploadForm.tsx
  - app/(protected)/board/meetings/[id]/resolutions/new/page.tsx
  - app/(protected)/board/meetings/[id]/resolutions/new/ResolutionForm.tsx
  - app/(protected)/board/meetings/[id]/actions/new/page.tsx
  - app/(protected)/board/meetings/[id]/actions/new/ActionItemForm.tsx
  - app/(protected)/board/actions/page.tsx (cross-meeting actions list)
  - components/board/AgendaList.tsx (optional)
  - components/board/ResolutionList.tsx
  - components/board/ActionItemList.tsx
  - components/board/DocumentList.tsx
autonomous: true
requirements:
  - BOARD-01
  - BOARD-02
  - BOARD-03
  - BOARD-04
must_haves:
  truths:
    - "Users with WRITE/RECORD roles can create/edit meetings, upload packs, record resolutions (only in_progress), create/update action items"
    - "Meeting detail uses shadcn Tabs for Agenda / Documents / Resolutions / Actions — all data in one place"
    - "Resolution numbers displayed as RES-001 (zero-padded)"
    - "Close Meeting button shows confirmation Dialog with irreversible warning"
    - "All forms use react-hook-form + Zod + shadcn components, browser SHA for uploads"
    - "Immutability banner shown on closed meetings; edit/resolution buttons disabled"
  artifacts:
    - path: "app/(protected)/board/meetings/page.tsx + MeetingsTable.tsx"
      provides: "Full meetings list with TanStack Table, status filters, create button"
    - path: "app/(protected)/board/meetings/[id]/page.tsx"
      provides: "Meeting detail with tabs, all sub-lists, status-gated action buttons"
    - path: "app/(protected)/board/meetings/new/MeetingForm.tsx + pages"
      provides: "Create form with dynamic agenda items (add/remove), attendee multi-select"
    - path: "app/(protected)/board/meetings/[id]/resolutions/new/ResolutionForm.tsx"
      provides: "Resolution recording form (proposer/seconder selects from attendees)"
    - path: "app/(protected)/board/meetings/[id]/documents/upload/DocumentUploadForm.tsx"
      provides: "Drag-drop upload with client SHA-256 computation"
  key_links:
    - from: "all meeting UI"
      to: "lib/board/actions.ts"
      via: "Server Actions called from forms"
    - from: "MeetingDetailTabs"
      to: "lib/board/queries.ts"
      via: "parallel fetches for documents, resolutions, actions"
---
<object ive>
Deliver the complete meeting lifecycle UI: list, create, detail (with tabs), edit, document upload, resolution recording, action item creation, and cross-meeting actions list. All forms, tables, and status transitions per the locked UI-SPEC and routing in D-40.

Purpose: Core user-facing workflows for BOARD-01–04. Board secretaries and members can now fully execute the meeting cycle end-to-end in the browser.

Output: Full route tree under /board/meetings/* and /board/actions, plus supporting client components.
</objective>

<execution_context>
@C:\Users\Kuziwa\.config\kilo\get-shit-done\workflows\execute-plan.md
@.planning/phases/05-board-management-meetings-and-resolutions/05-UI-SPEC.md
@.planning/phases/04-compliance-management-obligations-and-evidence/04-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-board-management-meetings-and-resolutions/05-CONTEXT.md

<interfaces>
From app/(protected)/compliance/obligations/page.tsx and [id]/page.tsx (TanStack + detail pattern):
```tsx
'use client'
import { useReactTable, ... } from '@tanstack/react-table'
export function ObligationsTable({ data }: { data: ObligationRow[] }) { ... }
```

From app/(protected)/compliance/obligations/[id]/evidence/upload/EvidenceUploadForm.tsx (browser SHA + dropzone):
```tsx
// uses window.crypto.subtle.digest('SHA-256', buffer)
// then form submit with hidden sha256 field
```

From components/ui/tabs.tsx, dialog.tsx, form.tsx, input, select, button (all installed per 05-UI-SPEC)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Meetings list + create meeting form + table (D-40, D-07, D-08, UI-SPEC)</name>
  <files>
app/(protected)/board/meetings/page.tsx,
app/(protected)/board/meetings/MeetingsTable.tsx,
app/(protected)/board/meetings/new/page.tsx,
app/(protected)/board/meetings/new/MeetingForm.tsx
  </files>
  <action>
Create the list page (Server) that fetches meetings and renders client TanStack table.

Create /new page + MeetingForm client component with:
- react-hook-form + zodResolver(meetingSchema)
- Dynamic agenda items: array of inputs with + / - buttons (min 1)
- Attendee selector: multi-select or chip input (fetch institution members via new query helper if needed, or simple text for v1 — discretion)
- Submit calls createMeeting Server Action
- Success toast + redirect to detail

Use exact spacing/typography from 05-UI-SPEC (28px heading, DM Sans, gold buttons, 44px touch targets).

  </action>
  <verify>
    <automated>ls app/\(protected\)/board/meetings/MeetingsTable.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>List page + table + new form + MeetingForm exist, form validates with Zod, dynamic agenda works, submission calls action and redirects.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Meeting detail page with Tabs (Agenda/Documents/Resolutions/Actions) + status banner + close dialog (D-40, D-25, UI-SPEC)</name>
  <files>
app/(protected)/board/meetings/[id]/page.tsx,
app/(protected)/board/meetings/[id]/MeetingDetailTabs.tsx
  </files>
  <action>
Create the detail Server Component that fetches meeting + all related (parallel queries), renders immutable CLOSED banner if closed (red, uppercase, per UI-SPEC), disabled buttons for closed meetings.

Uses <Tabs> from shadcn with 4 tabs:
- Agenda: numbered list from agenda_items array
- Documents: DocumentList component with upload link + download links (to /api/board/documents/[id])
- Resolutions: list + "Record Resolution" button (only if in_progress + RECORD role) → links to /resolutions/new
- Actions: list + "Add Action Item" button

Include <Dialog> for Close Meeting (irreversible warning per specifics in CONTEXT).

  </action>
  <verify>
    <automated>ls app/\(protected\)/board/meetings/\[id\]/page.tsx && npx tsc</automated>
  </verify>
  <done>Detail page loads all data, tabs switch without reload, closed meetings show banner and lock UI, close dialog present with correct copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Resolution form, Action Item form, Document upload form, edit meeting, cross actions list (D-19, D-23, D-40, browser SHA)</name>
  <files>
app/(protected)/board/meetings/[id]/resolutions/new/ResolutionForm.tsx,
app/(protected)/board/meetings/[id]/actions/new/ActionItemForm.tsx,
app/(protected)/board/meetings/[id]/documents/upload/DocumentUploadForm.tsx,
app/(protected)/board/meetings/[id]/edit/page.tsx,
app/(protected)/board/actions/page.tsx
  </files>
  <action>
Create the three form pages + components:

- ResolutionForm: selects proposer/seconder from meeting.attendee_ids (or names), radio cards for vote_outcome (per UI-SPEC min-h-80px), calls createResolution (enforces in_progress server-side)
- DocumentUploadForm: drop zone 120px, client SHA-256 via subtle.digest before submit (hidden input), calls uploadDocument
- ActionItemForm: owner select, due date, optional resolution link, calls createActionItem
- Edit meeting: prefilled form (only if not closed)
- /board/actions: full filterable list of all action items across meetings (TanStack)

All forms use consistent shadcn + validation + toasts + redirect on success. Resolution numbers formatted RES-003 in lists.

  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "all meeting forms compile"</automated>
  </verify>
  <done>All sub-forms and the cross-meeting actions list exist, client SHA works for uploads, resolution creation blocked on closed/in_progress via action, UI matches 05-UI-SPEC (vote cards, resolution numbering, dialog patterns).</done>
</task>

</tasks>

<threat_model>
... (form input validation, closed meeting tampering prevented at server)
</threat_model>

<verification>
End-to-end: create meeting → add agenda/attendees → upload pack (SHA verified) → start meeting → record resolution (RES-001) → create action → close meeting → verify immutable (no edit buttons, 409 on attempted change).
</verification>

<success_criteria>
- All routes under /board/meetings/* and /board/actions render
- Forms submit successfully and data appears in detail tabs
- Closed meetings are fully locked
- Resolution numbers zero-padded
- Client SHA computed and server re-verified
- Matches every visual/interaction rule in 05-UI-SPEC
</success_criteria>

<output>
Create 05-05-SUMMARY.md documenting any discretion choices (e.g. attendee selection UX, tab vs accordion).
</output>
